[tool.poetry]
name = "mcp-server"
version = "0.1.0"
description = "MCP server with Azure OpenAI + FastAPI routing"
authors = ["Your Name"]

[tool.poetry.dependencies]
python = "^3.9"
fastapi = "^0.115.0"
uvicorn = "^0.30.0"
openai = "^1.43.0"
requests = "^2.32.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
